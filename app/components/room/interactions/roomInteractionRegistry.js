// Future collectible / frame interactions register here. Keeping this separate
// prevents member content from changing the shared navigation implementation.
export const roomInteractionRegistry = Object.freeze({});
